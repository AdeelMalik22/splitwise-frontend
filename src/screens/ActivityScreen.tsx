import React from "react"; import {ScrollView,View,Text,StyleSheet} from "react-native"; import {Ionicons} from "@expo/vector-icons"; import {Screen,Header,Avatar} from "../components/ReferenceUI"; import {expenses} from "../data/mockData"; import {colors} from "../theme/colors";
export default function ActivityScreen(){return <Screen><Header title="Activity" right={<View style={{flexDirection:"row",gap:18}}><Ionicons name="search-outline" size={21}/><Ionicons name="options-outline" size={21}/></View>}/><ScrollView>{["Today","Yesterday","10 May","8 May"].map((date,idx)=><View key={date}><Text style={s.date}>{date}</Text>{expenses.slice(idx,idx+1).map(e=><View style={s.card} key={e.id}><Avatar initial={e.icon||"E"} size={36}/><View style={{flex:1,marginLeft:10}}><Text style={s.bold}>{e.detail}</Text><Text style={s.meta}>{e.title}</Text><Text style={s.meta}>Goa Trip · {e.split}</Text></View><Text style={s.amount}>{e.positive?"+₹2,000":"+₹600"}</Text></View>)}</View>)}</ScrollView></Screen>}; const s=StyleSheet.create({date:{fontSize:11,fontWeight:"600",marginTop:13,marginBottom:8},card:{minHeight:70,borderWidth:1,borderColor:colors.border,borderRadius:11,padding:10,flexDirection:"row",alignItems:"center",marginBottom:12},bold:{fontSize:12,fontWeight:"600"},meta:{fontSize:10,color:colors.textSecondary,marginTop:3},amount:{fontSize:12,fontWeight:"600"}});
